# Smoke, 2026-07-25

69 cases checked, 0 broken. Each one is a single promise, checked in process
against the library rather than against a running harness.

## engine

| held | case |
|---|---|
| yes | a copied substrate carries the population and not the index |
| yes | a gene that is not a number decodes to a legal one |
| yes | a genome decodes into the slots its shape promises |
| yes | a kernels slope is the slope of its own strength |
| yes | a law that senses nothing moves nothing |
| yes | a pair block lands in the row its position names |
| yes | a probe outlives the edits it does not depend on |
| yes | a seeded sim is bit reproducible |
| yes | a shell peaks at its own peak |
| yes | an inert shell cannot stretch the reach |
| yes | anchor values are exactly one hot |
| yes | every anchor pair calibrates a real norm |
| yes | every particle stays inside the box |
| yes | grid and all pairs neighbor walks agree |
| yes | hat memberships sum to one and activate at most two adjacent anchors |
| yes | memberships are the traits they were built from |
| yes | no legal law reaches far enough to fold into its own image |
| yes | periodic distance uses the minimum image |
| yes | the box shrinks as a genome asks to sense more |
| yes | the draw is reproducible and stays inside its range |
| yes | the index steps aside where it cannot help |
| yes | threaded and sequential stepping agree bit for bit |
| yes | trait seeding matches the logit histogram exactly |
| yes | uneven logits seed the shares they asked for |

## harness

| held | case |
|---|---|
| yes | a command reads its verb its numbers and its gene edits |
| yes | a gate pulls its own metric into a live reading |
| yes | a law edit keeps the world and a world edit replaces it |
| yes | a live reading matches the rollout it imitates |
| yes | a reading comes due on the cadence it was given |
| yes | a rollout pulls a wide genome back inside its own box |
| yes | a search refuses a config it cannot run |
| yes | a seed crosses the boundary intact |
| yes | a seeded search starts from what it was handed |
| yes | a survivor carries the genome and the descriptor it was scored on |
| yes | a watched search reports every generation and stops when told |
| yes | an edit lands inside what the box allows |
| yes | json leaves here readable by whoever asked |
| yes | the boundary refuses what it cannot read |
| yes | the catalog names every metric the tuner has |
| yes | the layout names every gene in the order the genome holds them |

## tuner

| held | case |
|---|---|
| yes | a clause rejects a sim below its floor and above its ceiling |
| yes | a cloud measured against itself reads as featureless |
| yes | a dependent metric finds its axes in the descriptor it is handed |
| yes | a gate on an unmeasured metric reads as failed |
| yes | a law that pulls both ways alike reads symmetric |
| yes | a log bin lands between the edges it was given |
| yes | a missing clause costs the structure score without erasing it |
| yes | a once metric holds its slots before the final sample |
| yes | a partial reading skips a once clause and still judges the rest |
| yes | a phase counts only where it loops the box |
| yes | a rollout gives up on a genome its gates keep rejecting |
| yes | a search keeps at most its capacity and finds the same thing twice |
| yes | a search says which way every candidate failed |
| yes | a uniform cloud is told apart from a clumped one at every scale |
| yes | an empty sim clears no clause with a floor |
| yes | descriptor width is the sum of the plans metrics |
| yes | every metric fills exactly the slots it declared |
| yes | every metric reads onto the shared axis |
| yes | folding averages what averages and takes the last of the rest |
| yes | novelty pays for distance from what has already been found |
| yes | only a plan that compares against uniform pays for a baseline |
| yes | plan names each metric once however many asked for it |
| yes | plan places every dependency ahead of its dependent |
| yes | plan pulls in what a wanted metric reads |
| yes | separate bodies stay separate until the scale reaches across |
| yes | some genome survives a rollout and fills its descriptor |
| yes | the motion metrics read the sample behind them |
| yes | the same genome measures identically twice |
| yes | the structure criterion tells genomes apart |
